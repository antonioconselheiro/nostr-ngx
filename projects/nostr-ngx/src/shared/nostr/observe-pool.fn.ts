import { AbstractSimplePool } from 'nostr-tools/abstract-pool';
import { Observable } from 'rxjs';

// TODO: implements this
export function observePool(pool: AbstractSimplePool): {
  connections: Observable<void>
} {
  return {} as any;
}
